import { CostcenterOwnerPipe } from './costcenter-owner.pipe';

describe('CostcenterOwnerPipe', () => {
  it('create an instance', () => {
    const pipe = new CostcenterOwnerPipe();
    expect(pipe).toBeTruthy();
  });
});
